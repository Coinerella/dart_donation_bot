version: '3.1'
services:
  dart_donation_bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dart_donation_bot
    volumes:
      - ./hive:/app/hive
    environment: 
      - DISCORD_API_TOKEN=""
      - DISCORD_CHANNEL_ID=""
      - DONATION_SCRIPT_HASH=""
      - DONATION_ADDRESS=""
      - ELECTRUM_SERVER_URL=""
      - SILENT_OPERATION=false
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: 100m